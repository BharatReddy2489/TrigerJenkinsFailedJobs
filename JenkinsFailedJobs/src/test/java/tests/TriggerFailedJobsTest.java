package tests;

import org.testng.annotations.Test;

public class TriggerFailedJobsTest extends BaseTest{
	
	@Test
	public void jenkinsTriggerFailedJobs() throws InterruptedException {
		//getTOFJ(user,pwd,crumb,domain);
		//triggerETEJobsWithFailedJobs(user,pwd,crumb,ETFJobNames);
		//verifyETEJobStatus(user,pwd,crumb);
		//triggerFailedJobs(user,pwd,crumb,ETFJobNames);
		//getTOFJTemp(user,pwd,crumb,domain);
		//triggerFailedJobstemp(user,pwd,crumb,ETFJobNames);
	}
	
	
	}
